# Portfolio Alert 后续迭代 TODO

本文档用于指导 Codex 后续迭代 `portfolio_alert` 项目。项目定位是：

> 面向个人长期 ETF / A股持仓的轻量级 Windows 持仓监控工具。

当前项目已经基本满足使用需求：可读取本地持仓、获取行情、计算组合市值和盈亏、判断交易时间、非交易时间显示最近一次组合状态，并支持低频刷新。后续迭代应以“稳定、清晰、少打扰、好维护”为原则，不要过度增加复杂功能。

---

## 一、当前项目状态

当前已有功能：

* 读取 `holdings.yaml`
* 读取 `config.yaml`
* 获取 ETF / A股行情
* 计算单只持仓市值、盈亏、收益率
* 计算组合总市值、总盈亏、总收益率
* 支持交易时间判断
* 支持 15 分钟刷新
* 非交易时间不高频请求行情
* 启动时打印当前监控状态
* 非交易时间显示最近一次可用组合状态
* 支持日志记录
* 支持 `.gitignore` 过滤缓存、日志、私有持仓文件

当前运行效果示例：

```text
Portfolio Alert 已启动
当前为非交易时间
启用持仓：4 只
刷新频率：15 分钟

非交易时间说明：
当前不再高频请求实时行情。
程序将在下一个交易时段自动刷新。

最近一次可用组合状态：
当前组合市值：17615.80 元
总成本：17387.80 元
总盈亏：+228.00 元
总收益率：+1.31%

下一次交易刷新时间：2026-06-03 09:30:00
```

---

## 二、后续迭代原则

请严格遵守以下原则：

1. 不接入券商交易接口；
2. 不实现自动买卖；
3. 不做高频交易工具；
4. 不做复杂 GUI；
5. 不在终端周期性刷屏；
6. 不把真实持仓、成本价、日志提交到 Git；
7. 优先保证程序稳定运行；
8. 优先提升日常使用体验；
9. 所有提示、日志说明、README 说明尽量使用中文；
10. 新增功能必须可通过配置关闭。

---

## 三、优先级 P1：收盘日报

### TODO 7：增加收盘日报

配置项：

```yaml
daily_report:
  enabled: true
  report_time: "15:05"
```

行为：

* 交易日收盘后推送一次日报；
* 每天只推送一次；
* 重启程序后不要重复推送同一天日报；
* 可通过本地状态文件记录已推送日期。

日报示例：

```text
【Portfolio Alert 收盘日报】

日期：2026-06-02
组合市值：17615.80 元
总成本：17387.80 元
总盈亏：+228.00 元
总收益率：+1.31%

持仓数量：4 只
涨幅最大：562500 机器人ETF +2.15%
跌幅最大：159819 人工智能ETF -1.20%
```

---

## 四、优先级 P1：提醒逻辑优化

### TODO 8：增加盈利提醒

配置项：

```yaml
alert:
  total_profit_threshold: 1000
  total_loss_threshold: -500
```

行为：

* 总盈利超过阈值时提醒；
* 总亏损超过阈值时提醒；
* 同类提醒需要冷却时间；
* 不要每次刷新都重复提醒。

### TODO 9：增加单只持仓提醒

在 `holdings.yaml` 中允许单独配置：

```yaml
holdings:
  - code: "562500"
    name: "机器人ETF"
    shares: 5300
    cost_price: 1.10
    enabled: true
    alert_profit: 800
    alert_loss: -500
```

规则：

* 单只持仓配置优先于全局配置；
* 未配置则使用全局配置；
* 所有提醒都需要冷却时间。

### TODO 10：增加快速波动提醒

配置项：

```yaml
alert:
  rapid_change:
    enabled: true
    window_min: 15
    percent_threshold: 3.0
```

行为：

* 记录最近若干次价格；
* 如果某只持仓 15 分钟内涨跌超过 3%，弹窗提醒；
* 不要因为缓存数据重复触发；
* 需要冷却时间。

---

## 五、优先级 P2：行情源容错

### TODO 11：增加备用行情源

当前主要使用 AKShare。

后续可增加：

* efinance
* easyquotation
* 本地缓存价格

建议接口统一为：

```python
get_latest_prices(codes: list[str]) -> dict[str, float]
```

内部优先级：

```text
AKShare ETF 接口
↓
AKShare A股接口
↓
备用数据源
↓
latest_snapshot 缓存价格
```

要求：

* 单个数据源失败不能导致程序崩溃；
* 不要打印长 Traceback 到终端；
* 详细错误写入日志文件；
* 最终结果中要标注价格来源。

示例输出：

```text
价格来源：AKShare
```

或：

```text
价格来源：缓存，时间：2026-06-02 15:01:00
```

---

## 六、优先级 P2：Windows 使用体验

### TODO 12：增加开机自启动说明

README 增加 Windows 开机自启动方法：

方案一：启动文件夹快捷方式

```text
Win + R
shell:startup
```

放入快捷方式：

```text
python E:\CodeLocal\StakeTool\portfolio_alert\run.py
```

方案二：任务计划程序

* 登录时启动；
* 工作目录设为项目目录；
* 使用 base conda 的 python.exe；
* 输出日志写入 `logs/`。

### TODO 13：增加 bat 启动脚本

新增：

```text
start_portfolio_alert.bat
```

内容示例：

```bat
@echo off
cd /d E:\CodeLocal\StakeTool\portfolio_alert
E:\application\anaconda\python.exe run.py
pause
```

注意：

* 路径应在 README 中说明需要用户自行修改；
* 不要硬编码用户真实路径到通用示例中；
* 示例可写成占位符。

### TODO 14：后续可选托盘模式

暂不优先实现。

若后续实现，可考虑：

```powershell
python run.py --tray
```

行为：

* 后台运行；
* 系统托盘图标；
* 右键菜单：

  * 查看状态
  * 手动刷新
  * 打开日志
  * 退出

托盘模式为可选项，不要影响当前命令行模式。

---

## 七、优先级 P2：测试与质量

### TODO 15：补充测试

至少增加：

* `test_snapshot.py`
* `test_scheduler.py`
* `test_alerts.py`
* `test_cli.py`

测试内容：

1. 交易时间内下一次刷新时间；
2. 午休时间下一次刷新时间；
3. 收盘后下一次刷新时间；
4. 周末下一次刷新时间；
5. snapshot 保存与读取；
6. holdings.yaml 缺失时的提示；
7. 缓存过期时提示；
8. 组合盈利阈值判断；
9. 组合亏损阈值判断；
10. 单只持仓提醒判断。

---

## 八、优先级 P2：README 更新

README 需要包含：

1. 项目简介；
2. 功能特性；
3. 安装依赖；
4. 如何配置 `holdings.yaml`；
5. 如何配置 `config.yaml`；
6. 如何运行；
7. 非交易时间行为说明；
8. latest_snapshot 缓存说明；
9. 日志文件说明；
10. 常见问题。

常见问题至少包括：

### 为什么非交易时间显示的是最近一次状态？

因为非交易时间程序不会频繁请求实时行情，会优先显示最近一次成功获取的组合快照。

### 为什么不做自动卖出？

本工具仅用于提醒，不接入交易接口，避免误交易风险。

### 为什么行情偶尔获取失败？

免费行情接口可能受网络、代理、接口限流、数据源调整影响。程序会记录日志并使用最近一次缓存状态。

---

## 九、暂不做的功能

以下功能暂不实现：

1. 自动交易；
2. 自动卖出；
3. 高频刷新；
4. 复杂 GUI；
5. K线图；
6. 技术指标；
7. 量化策略；
8. 回测系统；
9. 多账户管理；
10. 云端同步真实持仓。

这些功能会显著增加复杂度，且不符合当前项目定位。

---

## 十、推荐下一步执行顺序

建议 Codex 按以下顺序执行：

1. 增加收盘日报；
2. 增加盈利提醒；
3. 增加单只持仓提醒；
4. 更新 README；
5. 补充测试。

每次只做一到两个任务，不要一次性大规模重构。

---

## 十一、给 Codex 的工作要求

请在后续修改中遵守：

1. 用中文说明修改思路；
2. 用中文说明涉及文件；
3. 用中文说明测试方法；
4. 不要大改目录结构；
5. 不要删除已有功能；
6. 不要破坏当前可运行状态；
7. 每次修改后运行测试；
8. 如果需求不明确，先提出问题，不要自行假设过多；
9. 代码注释尽量使用中文；
10. README 面向普通 Windows 用户写，不要写得太 Linux 化。
